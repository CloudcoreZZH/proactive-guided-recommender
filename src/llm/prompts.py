"""Prompt 模板管理"""

# 电影描述生成 Prompt
MOVIE_DESCRIPTION_PROMPT = """请为电影《{title}》（类型：{genres}，年份：{year}）生成一段 200 字左右的详细描述，包括：
1. 剧情简介（基于你对这部电影的了解）
2. 主要主题和情感基调
3. 适合什么样的观众
返回纯文本，不要用 JSON 格式。"""

# 用户画像生成 Prompt
USER_PROFILE_PROMPT = """以下是一位用户最近观看的电影（按时间顺序）：
{movie_list}

请基于这些观看记录，生成一段简洁的用户兴趣画像描述（100字左右），包括：
1. 偏好的电影类型和风格
2. 可能感兴趣的主题方向
3. 观影品味特点

直接输出描述文本，不要用编号或列表格式。"""

# 推荐排序 Prompt
RANKING_PROMPT = """用户兴趣画像：{user_profile}

以下是为该用户筛选出的候选电影：
{candidate_movies_with_descriptions}

请从这些候选电影中选出最适合该用户的 {top_k} 部，按推荐度从高到低排列。
对每部电影用一句话（不超过30字）说明推荐理由。

严格按以下格式返回，每行一部电影：
1. [电影ID] 电影名 —— 推荐理由
2. [电影ID] 电影名 —— 推荐理由
..."""

# Serendipity 通道价值验证 Prompt
SERENDIPITY_PROMPT = """用户兴趣画像：{user_profile}

候选电影：{movie_title}
描述：{description}

这部电影与用户的直接兴趣不完全吻合，但请判断它是否有可能为用户带来：
- 新的视角
- 潜在的兴趣拓展
- 认知价值

如果你认为这部电影对用户有价值，返回"是"并给出一句话理由；
否则返回"否"。
"""
